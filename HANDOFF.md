# HANDOFF

The round-2 development log (asks 1-36, paper-side <-> server-side, 2026-07-05
to 2026-07-16) has been archived in full at `archiv/handoff/HANDOFF_round2.md`.
It documents the reasoning behind every round-2 methodology decision (wake
staleness bug fix, ERA5-BC negative finding, aging placebo test, etc.) and is
kept as the audit trail for reviewer responses.

## Open item (paused)

**Ask 36** (2026-07-16, never answered) requested:
- Renewables.ninja 10 m counterparts for the 40 stations added when the
  dataset grew from 160 to 200 sites, and a regeneration of the three 10 m
  comparison figures (`fig3.png`/`fig4.png`/`fig5.png`) on the full 200-site
  set. These figures currently still reflect the 160-site state (generated
  2026-07-02), while the rest of the paper/dataset already reflects 200 sites.
- A "wind park" -> "wind farm" terminology pass over figure labels.

This has been explicitly paused (not resolved, not implemented) — see the
archived log for full detail before resuming it.

Start a new dated log here for any future round; do not append to the
archived file.
