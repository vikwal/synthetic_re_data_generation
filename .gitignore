.DS_Store
.vscode
__pycache__
*.log
*.pyc

# raw/bulk data — never push data, see CLAUDE-facing note in README
data
synthre
literature
logs

# legacy code, superseded results/figures, and closed development logs —
# kept on disk for reference, never tracked
archiv

# working/raw outputs of scripts/round2 (regenerated on demand); the curated
# subset that matches the manuscript lives in the tracked results/ directory
# at repo root instead
figs

# development log (internal process, not for the public repo)
HANDOFF.md

# real, identifiable park data — never push: real PV/wind farm configs
# (owner-confidential), the rendered PV-park location map, and the
# scraped turbine power-curve database (redistribution rights unclear)
configs/real_pv_parks/
configs/round2/_generated/config_*.yaml
pv_parks.html
power_curves/*.csv
power_curves/Archiv/

test.ipynb
test.py
